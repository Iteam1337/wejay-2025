# Spotify OAuth Credentials
# Get these from https://developer.spotify.com/dashboard
VITE_SPOTIFY_CLIENT_ID=your_spotify_client_id_here
CLIENT_SECRET=your_spotify_client_secret_here

# Redirect URI options:
# Option 1: For local development (working solution)
# Add to Spotify Dashboard: http://127.0.0.1:8080/callback
VITE_SPOTIFY_REDIRECT_URI=http://127.0.0.1:8080/callback

# Option 2: For localtunnel (alternative)
# Run: npx localtunnel --port 8080
# Then set: VITE_SPOTIFY_REDIRECT_URI=https://your-random-subdomain.loca.lt/callback

# Option 3: For production
# VITE_SPOTIFY_REDIRECT_URI=https://yourdomain.com/callback

# Note: Spotify accepts 127.0.0.1 but not localhost for security reasons
# Note: CLIENT_SECRET is used by Vite backend proxy for token exchange

# Redis Configuration (for queue and playback state)
REDIS_HOST=localhost
REDIS_PORT=6379